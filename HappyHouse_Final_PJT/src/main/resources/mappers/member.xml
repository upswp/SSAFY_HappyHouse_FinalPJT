<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.mapper.MemberMapper">

	<select id="login" parameterType="MemberDto"
		resultType="MemberDto">
		select userno,userid,username,userpwd,email,address,birth_date,interest_area,marriage_type,no
		from member
		where userid =
		#{userid} and userpwd = #{userpwd}
	</select>
	
	
	<!-- 회원 전체 조회 -->
	<select id="retrieveMember" resultType="MemberDto">
		select userno,userid,username,userpwd,email,address,birth_date,interest_area,marriage_type,no
		  from member
		 order by no desc
	</select>
	
	<!-- 회원 상세 조회 -->
	<select id="detailMember" resultType="MemberDto">
		select userno,userid,username,userpwd,email,address,birth_date,interest_area,marriage_type,no
		  from member
		 where no = #{no}
	</select>

	<!-- 회원 등록 -->
	<insert id="joinMember" parameterType="MemberDto">
		insert into notice_board(
			userid,username,userpwd,email,address,birth_date,interest_area,marriage_type
		) values (
			#{userid}, #{username}, #{userpwd}, #{email},#{address},#{birth_date},#{interest_area},#{marriage_type},
		)
	</insert>
	
	<!-- 회원 수정 -->
	<update id="updateMember" parameterType="MemberDto">
		update member
		   set userpwd = #{userpwd}, 
		   email = #{email}, 
		   address = #{address}, 
		   birth_date = #{birth_date}, 
		   interest_area = #{interest_area}, 
		   marriage_type = #{marriage_type}, 
		 where no = #{no}
	</update>

	<!-- 회원 삭제 -->
	<delete id="deleteMember" parameterType="int">
		delete 
		  from member
		 where no = #{no}
	</delete>

</mapper>